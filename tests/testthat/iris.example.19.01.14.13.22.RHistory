class(iris)
ls()
data(iris)
ls()
iris.small <- iris[iris$Species %in% c("virginica", "versicolor")]
iris.small <- iris[iris$Species %in% c("virginica", "versicolor"),]
glm.out <- glm(Species ~ Sepal.Width + Sepal.Length + Petal.Width + Petal.Length,
               data = iris.small,
               family = binomial) # family = binomial required for logistic regression
summary(glm.out)
glm.out <- glm(Species ~ Sepal.Width + Petal.Width + Petal.Length,
               data = iris.small,
               family = binomial)
summary(glm.out)
plot(glm.out)
par(mfrow=c(2,2))
plot(glm.out)
library(backupSession)
save.session("iris.example", path="data")
